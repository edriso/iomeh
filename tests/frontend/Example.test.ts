import { describe, it, expect } from 'vitest';

// Simple example of how to test a component
describe('Example Test', () => {
    it('shows how to test something', () => {
        // This is just an example - you can delete this file
        expect(true).toBe(true);
    });
});
